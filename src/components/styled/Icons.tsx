import styled from "styled-components";

export const Checked = styled.span`
	width: 20px;
	height: 20px;
	background-color: var(--c-text-1);
	border-radius: 50%;
`;

export const UnChecked = styled.span`
	width: 20px;
	height: 20px;
	border: 3px solid var(--c-div);
	border-radius: 50%;
`;
